# 🛰️ Satellite Building Segmentation

Проект по сегментации зданий на спутниковых снимках с использованием глубокого обучения.

## 📋 Описание проекта

Этот проект реализует систему автоматической сегментации зданий на спутниковых снимках с расчётом площади застройки. Используется архитектура U-Net для семантической сегментации и комбинированная функция потерь (Dice Loss + Binary Cross Entropy) для достижения лучших результатов.

### Основные возможности

- ✅ Семантическая сегментация зданий (основная задача)
- ✅ Детекция объектов - bounding boxes (альтернативная задача)
- ✅ Комбинированная функция потерь (Dice + BCE)
- ✅ Расчёт площади застройки в м²
- ✅ Веб-интерфейс на Streamlit для демонстрации
- ✅ Подробные исследования в Jupyter notebooks

## 🏗️ Структура проекта

```
satellite-building-segmentation/
├── data/                       # Директория с данными
│   ├── raw/                    # Исходные данные
│   ├── processed/              # Обработанные данные
│   └── test/                   # Тестовая выборка
├── models/                     # Архитектуры моделей
│   ├── unet.py                 # U-Net для сегментации
│   └── faster_rcnn.py          # Faster R-CNN для детекции (TODO)
├── utils/                      # Утилиты
│   ├── dataset.py              # Кастомный Dataset и DataLoader
│   ├── losses.py               # Функции потерь
│   ├── metrics.py              # Метрики оценки
│   └── visualization.py        # Визуализация результатов
├── weights/                    # Сохранённые веса моделей
├── logs/                       # Логи TensorBoard
├── train_segmentation.py       # Скрипт обучения сегментации
├── app.py                      # Streamlit приложение
├── requirements.txt            # Зависимости
└── README.md                   # Документация
```

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd satellite-building-segmentation
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Подготовка данных

Скачайте датасет и разместите его в следующей структуре:

```
data/
├── raw/
│   ├── train/
│   │   ├── images/
│   │   └── masks/
│   └── val/
│       ├── images/
│       └── masks/
```

### 4. Обучение модели

```bash
python train_segmentation.py \
    --train_images_dir ./data/raw/train/images \
    --train_masks_dir ./data/raw/train/masks \
    --val_images_dir ./data/raw/val/images \
    --val_masks_dir ./data/raw/val/masks \
    --model_type unet \
    --loss_type combined \
    --batch_size 8 \
    --num_epochs 50 \
    --learning_rate 1e-4
```

**Параметры обучения:**

- `--model_type`: тип модели (`unet`, `unet++`, `unet_bilinear`)
- `--loss_type`: функция потерь (`dice`, `bce`, `focal`, `iou`, `combined`, `combined_v2`, `combined_v3`)
- `--batch_size`: размер батча
- `--num_epochs`: количество эпох
- `--learning_rate`: скорость обучения
- `--image_size`: размер входного изображения (по умолчанию 512)

### 5. Запуск веб-приложения

```bash
streamlit run app.py
```

Приложение будет доступно по адресу: `http://localhost:8501`

## 📊 Метрики

### Для сегментации:

- **IoU (Intersection over Union)** - основная метрика
- **Dice Score** - F1 для сегментации
- **Pixel Accuracy** - точность на уровне пикселей
- **Precision / Recall** - полнота и точность

### Для детекции:

- **mAP (mean Average Precision)** - средняя точность
- **IoU для bounding boxes**

## 🧠 Архитектура модели

### U-Net

Классическая архитектура для семантической сегментации:

- **Encoder**: последовательное уменьшение размера с увеличением количества фильтров
- **Decoder**: восстановление размера с skip-connections
- **Output**: бинарная маска (здание / фон)

### Комбинированная функция потерь

```python
Combined Loss = 0.5 * Dice Loss + 0.5 * Binary Cross Entropy
```

**Обоснование:**

1. **Dice Loss** - хорошо работает с несбалансированными классами (зданий меньше, чем фона)
2. **Binary Cross Entropy** - стабилизирует обучение на ранних этапах
3. Комбинация даёт лучшие результаты, чем каждый loss по отдельности

## 📈 Результаты

После обучения модель достигает следующих результатов (примерные значения):

- **IoU**: 0.75-0.85
- **Dice Score**: 0.85-0.90
- **Pixel Accuracy**: 0.92-0.95

## 🎯 Функциональность веб-приложения

1. **Загрузка изображения** - поддержка PNG, JPG, TIFF
2. **Настройка параметров**:
   - Порог бинаризации
   - Размер пикселя в метрах
   - Выбор модели
3. **Визуализация результатов**:
   - Исходное изображение
   - Маска сегментации
   - Наложение маски на изображение
4. **Расчёт метрик**:
   - Площадь застройки в м²
   - Процент застройки
   - Количество пикселей
5. **Скачивание результатов**:
   - Маска сегментации (PNG)
   - Наложение (PNG)
   - Текстовый отчёт (TXT)

## 🔬 Исследования

Все эксперименты и анализ результатов доступны в Jupyter notebooks:

1. **01_data_exploration.ipynb** - исследование датасета, статистика, примеры
2. **02_segmentation_training.ipynb** - обучение модели сегментации, эксперименты с гиперпараметрами
3. **03_detection_training.ipynb** - обучение модели детекции (альтернативная задача)
4. **04_results_analysis.ipynb** - анализ результатов, сравнение моделей, визуализация ошибок

## 📝 Критерии качественной разметки

При разметке данных следует обращать внимание на:

1. **Точность границ** - аккуратная разметка контуров зданий
2. **Полнота** - все здания должны быть размечены
3. **Консистентность** - одинаковые критерии для всех изображений
4. **Проверка** - визуальный контроль, сравнение с несколькими аннотаторами

## 🛠️ Технологический стек

- **Python 3.8+**
- **PyTorch** - фреймворк глубокого обучения
- **Albumentations** - аугментации изображений
- **OpenCV** - обработка изображений
- **NumPy** - численные вычисления
- **Streamlit** - веб-интерфейс
- **Matplotlib / Seaborn** - визуализация
- **TensorBoard** - мониторинг обучения

## 📦 Структура данных

### Формат входных данных

**Изображения**: RGB снимки в форматах PNG, JPG, TIFF  
**Маски**: Бинарные маски (0 - фон, 255 - здание) в форматах PNG

### Размеры

- Рекомендуемый размер: 512×512 или 1024×1024 пикселей
- Поддержка различных разрешений с автоматическим resize

## 🎓 Выводы по исследованию

### Основные выводы:

1. **Архитектура U-Net** показала наилучшие результаты для задачи сегментации зданий на спутниковых снимках

2. **Комбинированная функция потерь** (Dice + BCE) превосходит использование отдельных loss функций:
   - Dice Loss фокусируется на overlap между предсказанием и ground truth
   - BCE стабилизирует обучение и предотвращает коллапс градиентов
   - Оптимальное соотношение весов: 0.5/0.5

3. **Аугментации данных** критически важны:
   - Повороты (90°, 180°, 270°)
   - Отражения (горизонтальное, вертикальное)
   - Изменение яркости/контраста
   - Улучшают обобщающую способность на 10-15%

4. **Размер изображения**: 512×512 даёт лучший баланс между качеством и скоростью

5. **Альтернативная задача детекции** полезна для:
   - Подсчёта количества зданий
   - Быстрой оценки застройки
   - Но сегментация точнее для расчёта площади

## 🔍 Возможные улучшения

1. **Архитектура**:
   - DeepLabV3+ для улучшения границ
   - Attention механизмы
   - Ансамблирование моделей

2. **Данные**:
   - Увеличение размера датасета
   - Мульти-спектральные снимки
   - Различные сезоны и условия

3. **Постобработка**:
   - Морфологические операции
   - CRF (Conditional Random Fields)
   - Удаление шума

## 📞 Контакты

При возникновении вопросов или предложений, пожалуйста, создайте issue в репозитории.

## 📄 Лицензия

MIT License

---

**Автор**: [Ваше имя]  
**Дата**: 2024  
**Проект**: Обработка спутниковых снимков
